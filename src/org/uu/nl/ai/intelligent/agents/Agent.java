package org.uu.nl.ai.intelligent.agents;

public class Agent {

	public int MaximiseUtility() {
		int util = 0;
		
		// 1. For each period query the available courses
		// 2. Calculate utility taking into account preference and previous periods
		// - have a class for schedule which contains a class for period which contains 2 courses which will have name lecturer blabla from the knowledge base
		// and the calculated utility based on query???
		// - populate the schedule period by period, find a clash, start a new branch where sth is changed that clash (prerequisites needed for highest utility course)
		// - compare determined utilities
		// 3. Write max function
		
		return util;
	}
}
